
//#include "CRigidbody.h"
//#include "CCollider.h"
/*
//==Collision callbacks==
FORCE_INLINE void CCollider::OnCollide ( sCollision& collisionInfo )
{
	CGameBehavior* target = pRigidBody->GetTargetTransform()->pOwnerBehavior;
	if ( target ) {
		target->OnCollide( collisionInfo );
	}
}
FORCE_INLINE void CCollider::OnEnter ( sCollision& collisionInfo )
{
	CGameBehavior* target = pRigidBody->GetTargetTransform()->pOwnerBehavior;
	if ( target ) {
		target->OnCollisionEnter( collisionInfo );
	}
}
FORCE_INLINE void CCollider::OnLeave ( sCollision& collisionInfo )
{
	CGameBehavior* target = pRigidBody->GetTargetTransform()->pOwnerBehavior;
	if ( target ) {
		target->OnCollisionLeave( collisionInfo );
	}
}*/